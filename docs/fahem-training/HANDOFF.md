# فاهم — ملخّص انتقال (Handoff)

> نقطة استئناف لأي محادثة جديدة حول مستشار "فاهم" العقاري في rylist.

## أين وصلنا (الحالة الحالية)
- **فاهم = مستشار بشري شغّال حيّ.** النسخة المنشورة `fahem-chat` **edge v30** (2026-07-26).
- **⑤ إصلاح الإنجليزي (v30، commit `8660dcd`):** مراجعة المحادثتين المخزّنتين كشفت أن الإنجليزي مكسور — البرومبت المُشبَع بالعربية كان يحرف qwen فيرد **عربي** على مدخل إنجليزي، ويحرّف الاسم (Moayad→معتاد)، ويقحر كلمات عربية («Absher»)، ويتسرّب الحارس. الإصلاح (مُعمّم لغير العربي): تذكير لغة صارم لكل لغة (en + zh) يفرض لغة الرد + يعيد الحارس + يمنع القحر وتعريب الاسم؛ وإسقاط زوج الزيارة→الالتقاط من few-shot **لكل غير العربي**. مُتحقَّق حيًّا: en/zh يردّان بلغتهما بدفء، يحفظان الاسم، بلا التقاط مبكّر. العربي لم يُمسّ.
- **تحديث 2026-07-26 — أُنجزت المشاكل ١–٤ من قائمة الأولويات (commits على main: `46f359c` حارس التواصل، `22ef2d6` دعم الصينية، `ecccd3d` كود ميّت + onerror):**
  - **①** حارس صريح يمنع طلب الجوال/اختراع زيارة إلا عند طلب العميل الصريح للمضيّ قدمًا. مُتحقَّق حيًّا (عربي: العمولة/why-rylist يقفان نظيفَين؛ «حاب أزور» يلتقط).
  - **②** دعم الصيني كامل end-to-end: العقل (مدن/أنواع/افتتاحية/أخطاء/تلميحات صينية + 万) + الودجت (`lang()` يكتشف zh + نصوص). zh يعيد few-shot الإنجليزي **ناقص زوج الزيارة→الالتقاط** (كان يفرط في طلب الجوال) + تذكير لغة صيني يعيد تأكيد الحارس. البطاقات تُعرض بالصيني (البيانات الحيّة فيها ترجمة). مُتحقَّق حيًّا (v29).
  - **③** حُذف الكود الميّت `projectCode`/`interestCode` من الودجت.
  - **④** أُضيف `onerror` لصورة البطاقة (يُخفي حاوية الصورة عند كسر الرابط).
- **⚠️ الودجت (`fahem-chat.js`) لن يعمل الصيني على الموقع الحيّ حتى `git push main` → Vercel** (العقل edge v29 حيّ فعلًا، لكن نصوص الودجت + إرسال `language:"zh"` تحتاج نشر الموقع الثابت). **لم يُنفَّذ push بعد.**
- **#6 (تصحيح ذاكرة الموديل) = لا حاجة له:** الذاكرة أصلًا تذكر `qwen/qwen3.7-plus` بشكل صحيح؛ لا وجود لـ`gpt-4o-mini` في أي ملف ذاكرة.
- أثر بسيط متبقٍّ في الصيني: سؤال العمولة أحيانًا ينسب للعميل سياقًا سابقًا («ذكرت أنك تفكّر في…») — لا يمسّ الجوال/الزيارة، أدنى ضررًا.
- تطوّر عبر مراحل: بنشمارك 50 محادثة (درجة أساس 3.27/5) → إعادة تصميم لمحاور هجين → «مستشار بشري» (تعارف أولًا، بدون فورم، التقاط الجوال داخل الشات).
- **الصور والشاشة (تعديلاتك):** حيّة وسليمة — البطاقات ترجع بصور مشاريع حقيقية مرفوعة على Supabase storage؛ الودجت `fahem-chat.js` + صفحة `fahem.html` + اللانشر مربوطين صح.
- الفورم القديم (`request_contact`/`showContactForm`) **محذوف من الطرفين**. الالتقاط عبر كشف الجوال في النص (يشتغل حتى لو الذكاء وقف).
- أرشيف المحادثات في الأدمن (`fahem_conversations` + `admin/conversations.js`) مربوط ومطابق للودجت.

## الملفات الأساسية
- `supabase/functions/fahem-chat/index.ts` (546 سطر) — عقل فاهم. الموديل `qwen/qwen3.7-plus` عبر OpenRouter.
- `supabase/functions/fahem-chat/knowledge.ts` — FAQ + few-shot (تعارف-أولًا).
- `assets/js/fahem-chat.js` (247 سطر) — الودجت. `fahem.html` — صفحة الشات المخصّصة.
- `admin/conversations.js` — أرشيف المحادثات.
- `docs/fahem-training/` — البنشمارك (50 محادثة)، `evaluation.md` (3.27/5)، `knowledge-distilled.md`، `fahem-advisor-redesign.md`.

## مفاتيح تشغيل
- Supabase ref: `ghtcwsbtyvczlznviojj` (عبر `mcp__claude_ai_Supabase`).
- سرّ مطلوب: `OPENROUTER_API_KEY` (كان انقطع مرة بسبب rate-limit مؤقت ورجع).
- Endpoint اختبار: `POST https://ghtcwsbtyvczlznviojj.supabase.co/functions/v1/fahem-chat` (anon من `fahem-chat.js`).
- **نشر:** MCP فقط (`deploy_edge_function`) — لا يوجد supabase CLI/token محليًا. تحقّق بعد النشر بـ `get_edge_function`.
- **ملاحظة بيئة:** Git Bash محليًا لغته فاضية → `curl -d '{...عربي...}'` يفسد UTF-8؛ استخدم `--data-binary @file.json`.

## المتبقّي (مرتّب بالأولوية) — للجلسة الجاية
0. **🟠 push للموقع:** `git push main` لتفعيل الودجت الصيني + إصلاحات #3/#4 على الموقع الحيّ (Vercel). العقل edge v29 حيّ فعلًا، لكن هذي تغييرات ملفات ثابتة. **لم يُنفَّذ بعد.**
5. **قياس «بعد» (أهم متبقٍّ فعلي):** إعادة بنشمارك الـ50 محادثة لقياس الدرجة الجديدة مقابل 3.27 (لم يُنفَّذ بعد).
- (اختياري) منع الأثر البسيط: نسبة سياق سابق للعميل في الصيني عند سؤال العمولة.

**أُنجز (2026-07-26):** ①②③④ — انظر «أين وصلنا» أعلاه. #6 غير مطلوب (الذاكرة صحيحة أصلًا).

## ملاحظات git
- شغل فاهم مندمج في `main` (بدءًا من commit `66c0fe6` ثم مراحل أحدث حتى `b0de58b`).
- الشجرة نظيفة عدا ملفات غير متعلقة (`.gitignore`, `.claude/`, handoff قديم).
